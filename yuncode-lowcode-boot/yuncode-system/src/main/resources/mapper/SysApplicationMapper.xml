<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuncode.system.mapper.SysApplicationMapper">

    <!--
        分页查询应用列表
        支持按应用ID和应用名称搜索
    -->
    <select id="selectPage" resultType="com.yuncode.system.entity.SysApplication">
        SELECT *
        FROM sys_application
        WHERE deleted = 0
          AND tenant_id = #{tenantId}
          <if test="appName != null and appName != ''">
              AND (app_name LIKE CONCAT('%', #{appName}, '%')
                   OR app_id LIKE CONCAT('%', #{appName}, '%'))
          </if>
        ORDER BY create_time DESC
    </select>

    <!--
        根据ID查询应用
    -->
    <select id="selectById" resultType="com.yuncode.system.entity.SysApplication">
        SELECT *
        FROM sys_application
        WHERE id = #{id}
          AND deleted = 0
    </select>

    <!--
        检查应用ID是否存在
    -->
    <select id="existsByAppId" resultType="java.lang.Boolean">
        SELECT COUNT(*) > 0
        FROM sys_application
        WHERE app_id = #{appId}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </select>

    <!--
        根据 appId 和租户ID查询应用
    -->
    <select id="selectByAppId" resultType="com.yuncode.system.entity.SysApplication">
        SELECT *
        FROM sys_application
        WHERE app_id = #{appId}
          AND tenant_id = #{tenantId}
          AND deleted = 0
    </select>

    <!--
        物理删除（彻底删除）
    -->
    <delete id="physicalDeleteById">
        DELETE FROM sys_application WHERE id = #{id}
    </delete>

</mapper>
