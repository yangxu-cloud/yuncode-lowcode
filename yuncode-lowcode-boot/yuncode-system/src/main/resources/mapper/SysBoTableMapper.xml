<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuncode.system.mapper.SysBoTableMapper">

    <select id="selectByAppId" resultType="com.yuncode.system.entity.SysBoTable">
        SELECT * FROM sys_bo_table
        WHERE app_id = #{appId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectByCategory" resultType="com.yuncode.system.entity.SysBoTable">
        SELECT * FROM sys_bo_table
        WHERE app_id = #{appId} AND category_id = #{categoryId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <select id="selectCountByAppId" resultType="long">
        SELECT COUNT(*) FROM sys_bo_table
        WHERE app_id = #{appId} AND deleted = 0
    </select>

    <select id="selectCountByAppIdAndCategory" resultType="long">
        SELECT COUNT(*) FROM sys_bo_table
        WHERE app_id = #{appId} AND category_name = #{categoryName} AND deleted = 0
    </select>

    <update id="updateCategoryNameByCategoryId">
        UPDATE sys_bo_table
        SET category_name = #{categoryName}
        WHERE app_id = #{appId} AND category_id = #{categoryId} AND deleted = 0
    </update>

    <select id="selectCountByStorageName" resultType="long">
        SELECT COUNT(*) FROM sys_bo_table
        WHERE storage_name = #{storageName} AND deleted = 0
    </select>
</mapper>
