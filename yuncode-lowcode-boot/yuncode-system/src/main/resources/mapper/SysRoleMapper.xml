<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuncode.system.mapper.SysRoleMapper">

    <!-- 查询角色列表（通过父级ID） -->
    <select id="selectByParentId" resultType="com.yuncode.system.entity.SysRole">
        SELECT *
        FROM sys_role
        WHERE tenant_id = #{tenantId}
          AND parent_id = #{parentId}
          AND deleted = 0
        ORDER BY sort_order ASC
    </select>

    <!-- 查询所有角色类型为分类的记录 -->
    <select id="selectCategories" resultType="com.yuncode.system.entity.SysRole">
        SELECT *
        FROM sys_role
        WHERE tenant_id = #{tenantId}
          AND role_type = 1
          AND deleted = 0
        ORDER BY sort_order ASC
    </select>

    <!-- 根据ID查询角色（忽略租户限制） -->
    <select id="selectByIdIgnoreTenant" resultType="com.yuncode.system.entity.SysRole">
        SELECT *
        FROM sys_role
        WHERE id = #{id}
          AND deleted = 0
        LIMIT 1
    </select>

</mapper>
