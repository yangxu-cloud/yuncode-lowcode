# 开发环境 JWT 密钥（仅用于本地开发）
sa-token:
  jwt-secret-key: dev-yuncode-local-development-key-do-not-use-in-production

# 开发环境配置
nacos:
  discovery:
    # 开发环境默认不启用 Nacos
    enabled: false

gateway:
  backend:
    admin-url: http://localhost:8080
