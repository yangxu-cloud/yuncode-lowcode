# 技能名称

  你是一个专业的软件架构师和高级开发者，现在要设计sass系统的，组织服务功能，组织前端和后端代码结构，并且要能够构建微服务架构，为后期扩展做准备。


## 描述

    组织服务菜单挂接到菜单公共设施下面
    
    组织服务：
		1、将页面分为左右两部分显示
		2、左侧采用树形显示，用于展示现在系统组织
		3、再展开系统组织架构的同时，如果其下面有人员信息，也显示在展开菜单
		4、树型菜单具有操作按钮
		5、组织机构可以搜索,人也可以
		6、最顶层默认显示“组织架构”为默认层级
		7、“组织架构”下面只能添加公司/集团
		8，添加的时候有个字段叫做是否公司，是公司的话，下面只能添加部门
		9、操作按钮新增：是一个下拉列表，可以添加子部门和添加该部门人员
		10、添加页面是一个抽屉打开的表单
		11、左侧树应该有编辑，新增，和更多这几个操作，并且都用图标按钮
		12、为了实现同时显示人员和部门，我觉得你应该定义一个数据接口，将组织数据和人员数据同意转换成接口定义的字段
		13、添加sys_org,sys_user,sys_user_org，兼职的问题是不可避免的，但是我还是建议给用户表添加字段department_id，这个存储没有兼职的人员的部门，或者兼职人员的主部门，用来快速带出选择人员的部门
		14、给人员做一个添加兼职部门的功能，我们直接保存到sys_uer_org中，dept_id村的就是主部门，在人员表中也加一个is_leader的字段，看他是不是主部门的领导，还有角色id等
		15、首先搜索框，搜索的页面的树结构，他应该放到左侧树的顶部，不是页面的顶部
		16、在整个组织架构树里面，节点应该有三个类型，company,department,user三个状态，我们要控制数据和按钮。所以你因该建一个接口，规定组织机构树的数据接口

	要求:
		1、点击左侧组织机构的部门节点，右侧面板以tab标签页管理
			1.1 页签1显示基础信息，以一个简单的表格显示，包括主要包括名称
			1.2 基础信息页签的数据显示的时该部门/公司下的直接子级数据，也包括直接人员信息
			1.3 页签中有一个添加按钮，点击可以选择添加部门还是人员
			1.3 页签2显示的是该部门下的所有人员信息，包括所有孙子节点信息
		2、点击左侧组织机构的人员节点，右侧面板以tab标签页管理
			2.1 页签1显示人员基础信息，页签中有按钮，冻结（取消冻结），编辑，删除等按钮
			2.2 页签2显示该人员兼职的部门信息，包括兼职的岗位和角色
			2.3 页签3显示该人员所有用的菜单权限，可以进行勾选保存
			2.4 z排序号，能否自动设置排序号，当部门过多，不利于用户维护，能否增加左侧树的节点移动，自动计算排序号
		3、添加人员
			3.1 密码，我觉得首先默认密码我们要设置成可配置，用户输入的时候不让他输入密码，当用户使用默认密码登陆的时候，给他做一个提示，修改密码
		4、租户管理
			1、我们租户以公司/集团的方式管理
			2、当我们在根节点组织机构下添加公司的时候，让管理员输入租户编码和名称
			3、他下面的租户和部门都默认使用这个租户编码和名称
			4、我这样考虑的原因是组合管理和系统本身组织管理他是一样，区别就在于租户ID这些值，其实本身系统管理的这些也是一个特殊的租户
			推荐方案：平台管理员 + 租户管理员

			平台管理员（PLATFORM_ADMIN）
			↓
			- 可以添加公司（创建租户）
			- 可以查看所有租户数据
			- tenant_id = 0 或 NULL
			- 数量：1-3个（Yuncode 内部人员）

			租户管理员（TENANT_ADMIN）  
			↓
			- 可以管理本租户的组织、用户、角色
			- 不能添加公司（创建租户）
			- tenant_id = 具体租户ID
			- 数量：每个租户 1-N 个

			普通用户（NORMAL）
			↓
			- 根据角色权限访问功能
			- tenant_id = 具体租户ID

		5、节点树选择人员兼职功能开发

			(1)显示主部门（不可删除，但可以设置为负责人）
			(2)显示其他兼职部门（可以删除）
			(3)添加兼职部门时不能跨租户
			(4)让我先添加后端API获取用户的所
			(5)兼职部门的选择，做一个部门选择通用组件，组件可以通过租户过滤选择部门，并且通过参数控制多选和单选，组件分左右两列，左列显示部门树形结构，右侧显示常用信息
			(6)我移除他的兼职部门，整么能将他所有的组织关系都移除呢，他只移除要移除的部门和人员的关系就行

		6 操作日志
			现在日志记录了，但是有问题操作用户显示的2，操作描述无任何信息，请求方式空的，请求URL空的，浏览器，操作系统，耗时都是空的，状态我添加成功现实的也是失败，耗时难以统计，就不处理了

## 代码路径

    后台代码在路径，在当前目录下创建后台文件夹名称yuncode-lowcode-boot,前台yuncode-pure-admin
	
## 核心功能
   
    核心模块示例：根据成熟 SaaS 实践，应包含用户管理、角色管理、租户管理、权限控制 (RBAC)、短信/邮件管理、操作日志等
	高级扩展：如工作流引擎 (BPMN)、支付系统、数据报表、AI 大模型集成等
	
	一个合规的 SaaS 架构必须包含以下安全维度：
	• 访问层安全：包括网络访问控制、API 鉴权（防重放、防代码注入）以及 Web 安全检查。
	• 服务层安全：涉及网络区域逻辑隔离（业务网与管理网分离）、主机加固、入侵检测及恶意代码防护。
	• 数据安全：支持数据加密传输、隐私保护，以及在租户退租时彻底擦除存储资源中的相关数据。
	• 安全管理：包括统一的身份鉴别、安全审计日志（记录管理员和租户的所有敏感操作）以及密钥管理
	
	多用户登录采用sotoken+jwt实现，用户登录后，在前端存储sotoken，在后端存储jwt

	通知管理：采用SSE技术实现，前端订阅后端推送的事件


### 系统管理：

	1、系统要实现用户日志，系统日志，操作日志的分类，系统日志最好能够日志链路追踪，方便定位问题
	2、实现在线用户管理，能够对在线用户进行踢出操作
	3、日志链路追踪采用MDC + SkyWalkingAgent 技术
	4、统一日志管理，自动收集异常信息，并且采用合适的方案，记录用户日志、操作日志、系统日志，并且能够对这些日志进行检索和导出

### 系统监控：

	1、服务器监控：包含主机信息，服务器信息，jvm内存，系统内存，CPU，网络，磁盘吞吐量，磁盘信息等数据块
	2、数据库监控
	
## 项目实现方式

	每开发一个模块我需要给你提供一个对应的skill说明
	
## 参考代码

	你可以参考 C:\workspace\JeecgBoot-springboot3\JeecgBoot-springboot3下的代码分割
## 约束

    1、前端首选使用PureAdmin框架风格组件，你可以在当前项目中查找，或网络中查找
    2、在组件不满足的情况下，使用elemenet-plus组件库
    3、代码片段不能混淆和不能不宜读取和维护
    4、要有高级程序员的思路，封装和复用
	5、后台采用springboot，但是你要能很方便的构建微服务，为后期做准备
	6、前台采用pureadmin框架构建
	7、你的前端代码要符合pureadmin的风格，如果它满足不了需求，引入element-plus组件库
	8、代码中不能使用完全限定类名
	9、java代码要参考阿里java开发规范
	10、讨论和输出尽量都用中文
	11、maven路径C:\tools\apache-maven-3.9.12\bin
	12 jdk路径：C:\workspace\JeecgBoot-springboot3\jeecg-boot\jdk17\bin
    13、redis路径：C:\tools\redisbin_x64
	13、用中文交流

## github信息

	邮箱:471423287@qq.com
	用户名:yangxu-cloud
	仓库名:yuncode-lowcode

